{
  "name": "job-tracker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"vercel dev\" \"cd client && npm run dev\"",
    "install:all": "npm install --prefix client"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.4.0",
    "resend": "^3.2.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
